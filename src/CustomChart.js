import React from 'react';
import './CustomChart.sass';

const CustomChart = ({ children }) => {
    return (
        <div className='custom-chart'>
            {children}
        </div>
    )
}

export default CustomChart
